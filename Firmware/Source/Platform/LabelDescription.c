// Header
#include "LabelDescription.h"

// Variables
const Label BoardLabels[] =
{
		{"LSLControlBoard v.1.2 [Manufacturing]",	SID_PCB1_2_Manuf}
};
const Int16U BoardLabelsSize = sizeof(BoardLabels) / sizeof(BoardLabels[0]);
